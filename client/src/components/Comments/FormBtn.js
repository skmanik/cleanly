import React from "react";

export const FormBtn = props => (
  <div className="field">
    <p className="control">
      <button className="button is-info cl-postbutton" {...props}>Post comment</button>
    </p>
  </div>
);
